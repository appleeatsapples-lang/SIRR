# Production + CI dependencies — single source of truth.
# Railway installs from this file (repo root). CI installs from this file.
# Do NOT create Engine/requirements.txt — the drift caused the 2p.1 outage.

# Engine / astrology
pyswisseph
hijri-converter
hdate

# Document generation (reports, PDFs)
reportlab
openpyxl

# Web framework
fastapi
uvicorn[standard]
httpx
python-dotenv>=1.2.2  # CVE-2026-28684 (set_key symlink — not directly used, hygiene)

# Payments
stripe

# Path-C post-payment email (Launch — see Tools/handoff/LAUNCH_PATH_C_CUSTOM_EMAIL_BRIEF.md)
resend

# LLM narrative generation
anthropic

# §16.2 Tier 2 at-rest encryption
cryptography

# Rate limiting (PR #12) — per-IP limits on abuse-prone endpoints
slowapi

# Testing (needed in CI; prod containers will load but not use)
pytest>=9.0.3  # CVE-2025-71176 (/tmp pytest-of dir — test only, hygiene)
pytest-asyncio

# Transitive deps with security patches (pip-audit 2026-04-29 — none directly used in code, hygiene upgrades)
pillow>=12.2.0  # CVE-2026-25990 + CVE-2026-40192 (transitive via reportlab)
requests>=2.33.0  # CVE-2026-25645 (transitive)
